version: '3.8'
services:
  whatsapp-server:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./auth_info_baileys:/app/auth_info_baileys
      - ./media:/app/media
      - ./uploads:/app/uploads
    restart: unless-stopped
